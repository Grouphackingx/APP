{
  "name": "web-admin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web-admin",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project web-admin --web",
  "targets": {
    "dev": {
      "command": "next dev --port=4202",
      "options": {
        "cwd": "{projectRoot}"
      }
    }
  }
}
